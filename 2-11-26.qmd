---
title: "Week 3, Lecture 2"
author: "Camilla Rosour"
format: html
editor: visual
---

```{r}
library(ggplot2)
housing <- read.csv("https://jericholawson.github.io/classes/sds192/s26/data/housing.csv")
```

Question 1:

Observations = 20640

Variables = 27

Question 2:

```{r}
view(housing)
```

Categorical \<- ocean_proximity

Numerical \<- longitude, latitude, housing_median_age, total_rooms, total_bedrooms, populations, households, median_income, median_house_value

Question 3:

```{r}
library(ggplot2)
```

```{r}
ggplot(
  data = housing,
  mapping = aes(
    x = ocean_proximity
  )
) + 
  geom_bar() +
  labs(title = "Frequencies of Groups of Ocean Proximity
for California Disticts, 1990") +
  xlab("Ocean Proximity") +
  ylab("Frequency")
```

Question 4:

```{r}
ggplot(
  data = housing,
  mapping = aes(
    x = ocean_proximity,
  )
) + 
  geom_bar() +
  labs(title = "Frequencies of Groups of Ocean Proximity
for California Disticts, 1990") +
  xlab("Ocean Proximity") +
  ylab("Frequency") + 
  theme_minimal()
```

Question 5:
